# Copy to .env for docker compose.

# LLM provider: deepseek (recommended, direct API) or opencode-go.
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com
# OPENCODE_GO_API_KEY=your-opencode-go-api-key
# OPENCODE_GO_BASE_URL=https://opencode.ai/zen/go/v1

# Models: Flash for signals + backtest. Re-split with LLM_MODEL=deepseek-v4-pro when Pro upgrades.
LLM_MODEL=deepseek-v4-flash
LLM_MODEL_BACKTEST=deepseek-v4-flash

# pyserver Dockerfile defaults to TUSHARE_TOKEN="" (empty = free live sources:
# akshare+baostock). Leave this placeholder to achieve the same effect via .env,
# or set a real Tushare Pro token to enable secondary data.
TUSHARE_TOKEN=your-tushare-pro-token-here

# Live signals.
SIGNALS_LOAD_CONCURRENCY=3
SIGNALS_PYSERVER_TIMEOUT_MS=120000
SIGNALS_FUNDAMENTAL_TIMEOUT_MS=8000
SIGNALS_LLM_TIMEOUT_MS=900000
SIGNALS_LLM_MAX_ATTEMPTS=1
SIGNALS_LLM_SCORE_BATCH_SIZE=10

# Backtest.
BACKTEST_LOAD_CONCURRENCY=10
BACKTEST_PYSERVER_TIMEOUT_MS=60000
BACKTEST_LLM_SCORE_BATCH_SIZE=10
BACKTEST_SIGNAL_CONCURRENCY=8
BACKTEST_LLM_TIMEOUT_MS=300000
BACKTEST_LLM_MAX_ATTEMPTS=2

# Other scoreSymbols callers.
LLM_SCORE_BATCH_SIZE=10

# Universe refresh.
UNIVERSE_REFRESH_LLM_TIMEOUT_MS=900000
UNIVERSE_REFRESH_VALIDATE_TIMEOUT_MS=20000
# compose default 1 (enabled): ./web/data is bind-mounted so atomic UI
# refreshes persist on the host checkout. Set 0 to freeze the universe.
UNIVERSE_REFRESH_ENABLED=1
# Required when refresh is enabled. UI sends it as x-universe-refresh-token
# (also accepted as Authorization: Bearer). Generate a long random string.
UNIVERSE_REFRESH_TOKEN=change-me-universe-refresh-token

# Note: docker-compose.yml currently forwards only TUSHARE_TOKEN and PYSERVER_CACHE_DB
# to pyserver. To enable Tushare secondary, see docs/DEPLOY.md §2.
